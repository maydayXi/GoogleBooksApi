namespace ApiService.Enums;

/// <summary>
/// Defines the supported number of book records returned per page.
/// </summary>
public enum PageSize
{
    /// <summary>
    /// Returns 10 records per page.
    /// </summary>
    Ten = 10,

    /// <summary>
    /// Returns 20 records per page.
    /// </summary>
    Twenty = 20,
}